<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
	<springProperty scope="context" name="logPath" source="log.path"/>
	<!-- ==================== 控制台输出 ===================== -->
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>[%date] [%level] [%logger:%line] [%thread] traceLogid:[%X{TRACE_LOG_ID}] %msg%n</pattern>
		</encoder>
		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>DEBUG</level>
		</filter>
	</appender>

	<!-- INFO级别 -->
	<appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${log.path}/java-start-info.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<FileNamePattern>${log.path}/java-start-%d{yyyy-MM-dd}.%i.log</FileNamePattern>
			<MaxHistory>30</MaxHistory>
			<TimeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<maxFileSize>512MB</maxFileSize>
			</TimeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
		<encoder>
			<pattern>[%date] [%level] [%logger:%line] [%thread] [%X{TRACE_LOG_ID}] %msg%n</pattern>
            <charset>UTF-8</charset>
		</encoder>
	</appender>

	<!-- 异步日志 -->
	<appender name="ASYNC" class="ch.qos.logback.classic.AsyncAppender">
		<!-- 不丢失日志.默认的,如果队列的80%已满,则会丢弃TRACT、DEBUG、INFO级别的日志 -->
		<discardingThreshold >0</discardingThreshold>
		<!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
		<queueSize>1024</queueSize>
		<includeCallerData>true</includeCallerData>
		<!-- 添加附加的appender,最多只能添加一个 -->
		<appender-ref ref ="FILE"/>
	</appender>

	<logger name="org.apache.zookeeper" level="WARN"/>
	<logger name="org.springframework.beans" level="INFO"/>
	<!-- druid页面默认刷新5s会导致很多debug级别的无用日志打印 -->
	<logger name="org.springframework.boot.web.filter.OrderedRequestContextFilter" level="INFO"/>
	<logger name="com.alibaba.dubbo" level="WARN"/>

	<root>
		<level value="INFO" />
		<appender-ref ref="FILE" />
		<appender-ref ref="stdout" />
	</root>

</configuration>
